import 'whatwg-fetch';
import findId from './findMembershipId';

// import getMilestones from './milestones';

// Finds bungie ID for a given users gamertag
findId();

// Gets a list of milestone hashes, stores them in an array then loops through the array
// and makes a new fetch request with the specific hash for each one.
// getMilestones();
